#include "http.h"

static err_t get(http_request *req, http_response *resp)
{
  
}

static err_t post(http_request * req, http_response * resp);


void new_simple_http_client(struct http_client * result)
  

